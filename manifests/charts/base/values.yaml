# Default values for agentcube.

# Global settings
imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

# Redis Configuration
# These must be provided by the user during installation
redis:
  addr: ""
  # Chart-managed Secret from this value when secretName is unset. Do not set both password and secretName.
  password: ""
  # User-provided Secret name. When set, the chart does not create {release}-redis.
  secretName: ""
  # Key in the Secret (chart-managed or secretName). Default: password.
  secretKey: "password"

# AgentCube Router
router:
  replicas: 1
  image:
    repository: ghcr.io/volcano-sh/agentcube-router
    pullPolicy: IfNotPresent
    tag: "latest"
  service:
    type: ClusterIP
    port: 8080
    targetPort: 8080
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 128Mi
  config: {}
  extraEnv: []
  serviceAccountName: "agentcube-router"

# AgentCube Workload Manager
workloadmanager:
  replicas: 1
  image:
    repository: ghcr.io/volcano-sh/workloadmanager
    pullPolicy: IfNotPresent
    tag: "latest"
  service:
    type: ClusterIP
    port: 8080
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 128Mi
  extraEnv: []

# AgentCube AgentD (node daemon for SnapStart snapshot builds)
# Enable on nodes that have Kuasar installed and should participate in snapshot builds.
agentd:
  enabled: false
  image:
    repository: ghcr.io/volcano-sh/agentd
    pullPolicy: IfNotPresent
    tag: "latest"
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 64Mi
  # nodeSelector targets nodes where the runtime driver is installed.
  nodeSelector: {}
  extraEnv: []
  extraVolumes:
    - name: kuasar-admin-socket
      hostPath:
        path: /run/vmm-sandboxer-admin.sock
        type: Socket
  extraVolumeMounts:
    - name: kuasar-admin-socket
      mountPath: /run/vmm-sandboxer-admin.sock

# Volcano Agent Scheduler
volcano:
  scheduler:
    enabled: false
    replicas: 1
    image:
      repository: ghcr.io/volcano-sh/vc-agent-scheduler
      pullPolicy: IfNotPresent
      tag: "latest"

# SPIRE Configuration (Internal Workload Identity)
spire:
  enabled: false
  trustDomain: "cluster.local"
  clusterName: "agentcube-cluster"
  spiffeHelper:
    # Configuration for the spiffe-helper sidecar which automatically fetches and rotates mTLS certificates
    image:
      repository: ghcr.io/spiffe/spiffe-helper
      tag: "0.10.0"
      pullPolicy: IfNotPresent
    # Directory inside Router and WorkloadManager pods where the sidecar will deliver the certificates
    certDir: "/run/spire/certs"
    # Desired file names for the SVID certificate, private key, and root trust bundle
    certFileName: "svid.pem"
    keyFileName: "svid_key.pem"
    bundleFileName: "svid_bundle.pem"
  server:
    image:
      repository: ghcr.io/spiffe/spire-server
      tag: "1.12.0"
      pullPolicy: IfNotPresent
    service:
      type: ClusterIP
      port: 8081
    ca:
      ttl: "24h"
      x509SvidDefaultTtl: "1h"
    dataStore:
      type: "sqlite3"
      connectionString: "/run/spire/data/datastore.sqlite3"
    resources:
      limits:
        cpu: 200m
        memory: 512Mi
      requests:
        cpu: 50m
        memory: 128Mi
  agent:
    # For local dev clusters (kind/minikube), override with: --set spire.agent.insecureBootstrap=true --set spire.agent.skipKubeletVerification=true
    insecureBootstrap: false
    skipKubeletVerification: false
    image:
      repository: ghcr.io/spiffe/spire-agent
      tag: "1.12.0"
      pullPolicy: IfNotPresent
    resources:
      limits:
        cpu: 200m
        memory: 256Mi
      requests:
        cpu: 50m
        memory: 64Mi
  controllerManager:
    image:
      repository: ghcr.io/spiffe/spire-controller-manager
      tag: "0.6.4"
      pullPolicy: IfNotPresent
    resources:
      limits:
        cpu: 100m
        memory: 128Mi
      requests:
        cpu: 50m
        memory: 64Mi
