apiVersion: apps/v1
kind: Deployment
metadata:
  name: workloadmanager
  namespace: {{ .Release.Namespace }}
  labels:
    app: workloadmanager
spec:
  replicas: {{ .Values.workloadmanager.replicas }}
  selector:
    matchLabels:
      app: workloadmanager
  template:
    metadata:
      labels:
        app: workloadmanager
    spec:
      serviceAccountName: workloadmanager
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        {{- if .Values.spire.enabled }}
        - name: spiffe-helper
          image: "{{ .Values.spire.spiffeHelper.image.repository }}:{{ .Values.spire.spiffeHelper.image.tag }}"
          imagePullPolicy: {{ .Values.spire.spiffeHelper.image.pullPolicy }}
          args:
            - "-config"
            - "/etc/spiffe-helper/spiffe-helper.conf"
          volumeMounts:
            - name: spiffe-helper-config
              mountPath: /etc/spiffe-helper
              readOnly: true
            - name: spire-agent-socket
              mountPath: /run/spire/sockets
              readOnly: true
            - name: spire-certs
              mountPath: {{ .Values.spire.spiffeHelper.certDir }}
        {{- end }}
        - name: workloadmanager
          {{- if or .Values.spire.enabled .Values.workloadmanager.webhook.port }}
          volumeMounts:
            {{- if .Values.spire.enabled }}
            - name: spire-certs
              mountPath: {{ .Values.spire.spiffeHelper.certDir }}
              readOnly: true
            {{- end }}
            {{- if .Values.workloadmanager.webhook.port }}
            - name: webhook-tls-certs
              mountPath: /etc/workloadmanager/webhook-tls
              readOnly: true
            {{- end }}
          {{- end }}
          image: "{{ .Values.workloadmanager.image.repository }}:{{ .Values.workloadmanager.image.tag }}"
          imagePullPolicy: {{ .Values.workloadmanager.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.workloadmanager.service.port }}
              protocol: TCP
            {{- if .Values.workloadmanager.webhook.port }}
            - name: webhook-https
              containerPort: {{ .Values.workloadmanager.webhook.port }}
              protocol: TCP
            {{- end }}
          env:
            - name: AGENTCUBE_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: REDIS_PASSWORD
              value: {{ .Values.redis.password | quote }}
            - name: REDIS_ADDR
              value: {{ .Values.redis.addr | quote }}
            {{- with .Values.workloadmanager.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          args:
            - --port={{ .Values.workloadmanager.service.port }}
            - --runtime-class-name=
            {{- if .Values.workloadmanager.webhook.port }}
            - --webhook-port={{ .Values.workloadmanager.webhook.port }}
            - --webhook-tls-cert=/etc/workloadmanager/webhook-tls/tls.crt
            - --webhook-tls-key=/etc/workloadmanager/webhook-tls/tls.key
            {{- end }}
          resources:
            {{- toYaml .Values.workloadmanager.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.workloadmanager.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.workloadmanager.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
      {{- if or .Values.spire.enabled .Values.workloadmanager.webhook.port }}
      volumes:
        {{- if .Values.spire.enabled }}
        - name: spire-agent-socket
          hostPath:
            path: /run/spire/sockets
            type: DirectoryOrCreate
        - name: spiffe-helper-config
          configMap:
            name: spiffe-helper-config
        - name: spire-certs
          emptyDir: {}
        {{- end }}
        {{- if .Values.workloadmanager.webhook.port }}
        - name: webhook-tls-certs
          secret:
            secretName: {{ .Values.workloadmanager.webhook.tls.secretName }}
        {{- end }}
      {{- end }}

---
apiVersion: v1
kind: Service
metadata:
  name: workloadmanager
  namespace: {{ .Release.Namespace }}
  labels:
    app: workloadmanager
spec:
  type: {{ .Values.workloadmanager.service.type }}
  ports:
    - port: {{ .Values.workloadmanager.service.port }}
      targetPort: {{ .Values.workloadmanager.service.port }}
      protocol: TCP
      name: http
  selector:
    app: workloadmanager
